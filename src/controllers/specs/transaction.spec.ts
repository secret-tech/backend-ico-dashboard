import * as chai from 'chai';
const { expect } = chai;
import { Transaction } from '../../entities/transaction';

describe('Transaction Entity', () => {
  describe('create', () => {
    expect(true).eq(true);
  });
});
